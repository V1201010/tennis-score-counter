# Tennis Score Counter

PWA-приложение для подсчёта очков в теннисе. Работает офлайн.

## Запуск локально

Открыть `index.html` в браузере или использовать любой статический сервер:

```bash
npx serve .
```

## Деплой

Push в `main` → GitHub Pages / Vercel деплоит автоматически.
